import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    root: {
        background: 'blue',
    },
    imgLogo: {
        width: 80,
    },
    title: {
        fontWeight: 'bold',
    },
}));
export default useStyles;
