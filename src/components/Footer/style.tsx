import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    root: {
        background: 'blue',
        fontWeight: 'bold',
        paddingTop: 20,
        paddingBottom: 20,
    },
}));
export default useStyles;
